[project]
name = "devops-mcp"
version = "0.1.0"
description = "Audited MCP control plane for production VPS operations"
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastmcp==3.4.4",
    "starlette==1.3.1",
    "uvicorn==0.51.0",
]

[dependency-groups]
dev = [
    "pytest==9.0.2",
]

[tool.uv]
package = false
